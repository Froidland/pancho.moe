---
interface Props {
	href?: string;
	title?: string;
	"aria-label"?: string;
}

const { href, title, "aria-label": ariaLabel } = Astro.props;
---

<a
	{href}
	{title}
	aria-label={ariaLabel}
	class="underline underline-offset-2 hover:border-[#b4b5b6] hover:text-[#b4b5b6]"
	><slot /></a
>
